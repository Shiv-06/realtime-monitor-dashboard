## Realtime Monitoring Dashboard

A React-based dashboard that visualizes simulated system metrics (CPU, memory usage, request count) in near real-time.

## 🧠 Tech Stack

- React (functional components, hooks)
- JavaScript / TypeScript
- Chart.js or Recharts (for graphs)

## ✨ Features

- Line chart showing metric changes over time
- Auto-refresh every few seconds (using `setInterval` or hooks)
- Responsive layout

## Future Enhancements

- Connect to real APIs
- Add alerts when metrics cross thresholds
